RCM WEB v3.14 — COMBINED FINAL CLEANUP

Changes:
- Protects the currently signed-in admin from deactivating, deleting, or changing their own role. Controls are visibly disabled.
- Keeps the hidden 10xLABZ system_admin behavior from v3.13.
- Adds PWA/home-screen metadata and Rodriguez icon assets (192/512).
- Google Drive documents render with the Google Drive source badge, including a fallback for existing Drive links.
- Choosing an existing Google Drive file now opens a preview/confirmation modal before attaching. Images preview directly; other file types show file metadata plus Open in Google Drive.
- Existing desktop dashboard layout is untouched.

Cache version: cleanup314
